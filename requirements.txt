# ============================================================
# GeoVision-CLIP Cali — Dependencias completas
# Instalacion: pip install -r requirements.txt
# Entorno: /home/mrsasayo_mesa/venv_global/
# ============================================================

# ── DAGMA descarga ──
requests>=2.31
pandas>=2.0
pyarrow>=14.0               # Parquet (consolidacion)

# ── S5P GEE ──
earthengine-api>=1.7

# ── ERA5 CDS ──
cdsapi>=0.7

# ── MODIS MAIAC ──
earthaccess>=0.12

# ── Sentinel-2 AWS STAC ──
(no necesita paquetes extra)

# ── EDA — visualizacion ──
matplotlib>=3.8
seaborn>=0.13
numpy>=1.26
scipy>=1.12
rasterio>=1.3
geopandas>=0.14
folium>=0.17
scikit-learn>=1.4
statsmodels>=0.14
contextily>=1.6
cartopy>=0.22

# ── Procesamiento ──
dask[complete]>=2024.1
netcdf4>=1.7
h5py>=3.10
xarray>=2024.1

# ── Utilidades ──
tqdm>=4.66
psutil>=5.9
